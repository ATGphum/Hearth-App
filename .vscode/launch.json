{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to Docker",
      "port": 9229,
      "address": "localhost",
      "localRoot": "${workspaceFolder}/hearth-api",
      "remoteRoot": "/usr/src/app", // Path inside the Docker container
      "protocol": "inspector"
    }
  ]
}
